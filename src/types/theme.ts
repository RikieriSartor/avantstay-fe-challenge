export interface Theme {
  palette: {
    common: {
      white: string;
      gray: string;
      black: string;
    };
    primary: {
      light: string;
      main: string;
    };
    secondary: {
      light: string;
      main: string;
      dark: string;
    };
  };
}
